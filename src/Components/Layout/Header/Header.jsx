import React from "react";
import {UIHeader} from '../../UI/UIHeader/UIHeader';

export const Header =() =>{
    return(
        <UIHeader greatting="Consumo de api de Rick and Morty con filtro"/>
    );
}